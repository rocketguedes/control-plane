apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: headscale
resources:
  - certificate.yaml
  - deployment.yaml
  - persistentvolumeclaim.yaml
  - secret.yaml
  - service.yaml
  - serviceaccount.yaml
  - tlsroute.yaml
configMapGenerator:
  - name: headscale-config
    files:
      - config.yaml
images:
  - name: docker.io/headscale/headscale
    newTag: v0.29.3 # {"$imagepolicy": "flux-system:headscale:tag"}
    digest: sha256:0e7f1c6e4ce6c2a2a001103ecd3fa645a045adf30ac8a5234fe037b43000cd72 # {"$imagepolicy": "flux-system:headscale:digest"}
labels:
  - pairs:
      app.kubernetes.io/part-of: headscale
      app.kubernetes.io/version: v0.29.3 # {"$imagepolicy": "flux-system:headscale:tag"}
