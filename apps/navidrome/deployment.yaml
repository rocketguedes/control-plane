apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  labels:
    app.kubernetes.io/name: navidrome
    app.kubernetes.io/instance: navidrome
spec:
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: navidrome
      app.kubernetes.io/instance: navidrome
  template:
    metadata:
      labels:
        app.kubernetes.io/name: navidrome
        app.kubernetes.io/instance: navidrome
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "4533"
        prometheus.io/scrape: "true"
    spec:
      serviceAccountName: navidrome
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        disk: local
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: navidrome-plugins-init
          image: docker.io/deluan/navidrome
          command:
            - /bin/sh
          args:
            - -c
            - |
              set -eu

              mkdir -p /data/plugins

              fetch_plugin() {
                local name=$1
                local version=$2
                local checksum=$3
                local url=$4

                local target="/data/plugins/${name}.ndp"
                local version_file="${target}.version"
                local tmp="${target}.tmp.ndp"

                if [ -f "$target" ] && [ "$(cat "$version_file" 2>/dev/null)" = "$version" ]; then
                  return 0
                fi

                trap 'rm -f "$tmp"' EXIT

                wget -qO "$tmp" "$url"
                echo "${checksum}  ${tmp}" | sha256sum -c -s
                navidrome --nobanner plugin validate "$tmp"
                mv "$tmp" "$target"
                echo "$version" >"$version_file"

                trap - EXIT
              }

              fetch_plugin "apple-music" "$PLUGINS_APPLE_MUSIC_VERSION" "$PLUGINS_APPLE_MUSIC_SHA256" "https://github.com/navidrome/apple-music-plugin/releases/download/${PLUGINS_APPLE_MUSIC_VERSION}/apple-music.ndp"

              fetch_plugin "audiomuseai" "$PLUGINS_AUDIOMUSEAI_VERSION" "$PLUGINS_AUDIOMUSEAI_SHA256" "https://github.com/NeptuneHub/AudioMuse-AI-NV-plugin/releases/download/${PLUGINS_AUDIOMUSEAI_VERSION}/audiomuseai.ndp"

              if [ ! -f /data/navidrome.db ]; then
                navidrome --nobanner artwork status
              fi

              navidrome --nobanner plugin rescan
              navidrome --nobanner plugin enable apple-music
              navidrome --nobanner plugin edit audiomuseai --config="{\"apiToken\": \"$AUDIOMUSE_API_TOKEN\", \"apiUrl\": \"$AUDIOMUSE_API_URL\"}"
              navidrome --nobanner plugin enable audiomuseai
          envFrom:
            - secretRef:
                name: navidrome
          env:
            - name: AUDIOMUSE_API_URL
              value: http://audiomuse-ai-flask-service.audiomuse-ai.svc.cluster.local:8000
            - name: ND_LOGLEVEL
              value: warn
            - name: PLUGINS_APPLE_MUSIC_VERSION
              value: v0.2.0
            - name: PLUGINS_APPLE_MUSIC_SHA256
              value: 3682751e72caa5cc46b3f7ab70de70ea026f0a38a47f780b2d2b4922ed0ad154
            - name: PLUGINS_AUDIOMUSEAI_VERSION
              value: v10
            - name: PLUGINS_AUDIOMUSEAI_SHA256
              value: c138e9af16f097d8cd07ac957fb8a49d91a7da066e5b4a1457cadbd479aa10a7
          volumeMounts:
            - name: navidrome
              mountPath: /data
              subPath: data
            - name: config
              mountPath: /data/navidrome.toml
              subPath: navidrome.toml
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 32Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      containers:
        - name: navidrome
          image: docker.io/deluan/navidrome
          envFrom:
            - secretRef:
                name: navidrome
          ports:
            - name: http
              containerPort: 4533
              protocol: TCP
          volumeMounts:
            - name: navidrome
              mountPath: /data
              subPath: data
            - name: navidrome
              mountPath: /music
              subPath: library/apple-music
              readOnly: true
            - name: navidrome
              mountPath: /backup
              subPath: backup
            - name: config
              mountPath: /data/navidrome.toml
              subPath: navidrome.toml
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1536Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          startupProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: config
          configMap:
            name: navidrome
        - name: navidrome
          persistentVolumeClaim:
            claimName: navidrome
        - name: tmp
          emptyDir: {}
  strategy:
    type: Recreate
