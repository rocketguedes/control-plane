apiVersion: apiserver.config.k8s.io/v1
kind: AuthenticationConfiguration
jwt:
  - issuer:
      url: https://accounts.guedescloud.com.br
      audiences:
        - headlamp
    claimMappings:
      username:
        claim: "sub"
        prefix: ""
      groups:
        claim: "groups"
        prefix: ""
